{
  "version": "1.0",
  "description": "Agent 配置文件",
  "orchestrator": {
    "intent_detection": {
      "method": "llm",
      "description": "意图识别方法: llm (使用LLM) 或 keyword (关键词匹配)",
      "fallback_to_keyword": true,
      "description_fallback": "LLM 失败时是否回退到关键词匹配"
    }
  },
  "guide_agent": {
    "skill_matching": {
      "method": "trigger",
      "description": "Skill 匹配方法: trigger (触发词匹配) 或 llm (LLM 匹配)"
    }
  }
}
